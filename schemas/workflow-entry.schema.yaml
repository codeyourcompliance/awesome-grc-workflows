$schema: "https://json-schema.org/draft/2020-12/schema"
$id: "https://github.com/codeyourcompliance/awesome-grc-workflows/schemas/workflow-entry.schema.yaml"
title: Awesome GRC Workflow Entry Frontmatter

type: object
additionalProperties: false
required:
  - id
  - title
  - domains
  - demand_status
  - first_observed
  - last_reviewed
  - independent_demand_sources
  - contextual_sources
  - challenge_status
properties:
  id:
    type: string
    pattern: "^[a-z0-9]+(?:-[a-z0-9]+)*$"
  title:
    type: string
    minLength: 5
  domains:
    type: array
    minItems: 1
    uniqueItems: true
    items:
      type: string
      enum:
        - access-governance
        - ai-governance
        - cloud-assurance
        - compliance
        - continuous-control-monitoring
        - evidence-engineering
        - incident-review
        - it-audit
        - operational-resilience
        - remediation-validation
        - technology-risk
        - third-party-risk
  demand_status:
    type: string
    enum:
      - observed
      - repeated
      - candidate
  first_observed:
    type: string
    format: date
  last_reviewed:
    type: string
    format: date
  independent_demand_sources:
    type: integer
    minimum: 1
  contextual_sources:
    type: integer
    minimum: 0
  challenge_status:
    type: string
    enum:
      - none
      - proposed
      - challenge-ready
      - pilot-tested
      - validated-pattern