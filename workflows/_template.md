# Legacy Workflow Template — Do Not Use

This template belonged to the pre-v2.1 public workflow model.

Do not create new files from it. New public contributions must use [`cases/_template/`](../cases/_template/README.md).

The file is retained only to preserve repository history and explain the prior structure. A new v2.1 case must not expose or depend on named-employer research and must include a fictional scenario, synthetic inputs, professional deliverables, scoring rubric, reviewer guide, reference answer, metadata, and an explicit human-authority boundary.
